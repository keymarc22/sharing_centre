<div class="welcome-card">
  <div class="welcome-content">
    <div class="welcome-text">
      <h2 class="welcome-title"><%= t('views.dashboard.welcome', name: current_user.name) %></h2>
      <p class="welcome-message">
        New French speaking classes are available.<br>
        Etudier en France for B1 and B2 levels.
        <span class="learn-more"><%= t('views.dashboard.learn_more') %></span>
      </p>
      <button class="buy-button"><%= t('views.dashboard.buy_lesson') %></button>
    </div>
    <div class="welcome-image">
      <%= lucide_icon "book-open", class: "book-icon" %>
    </div>
  </div>
</div>
